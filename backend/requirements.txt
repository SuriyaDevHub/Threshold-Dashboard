fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.9
# --- Rule Designer: YAML is the source-of-truth store; ruamel round-trips
# comments/ordering (spec requirement to preserve them where possible).
ruamel.yaml>=0.18
# --- live mode only (S3 trade reads); not needed when USE_MOCK=true ---
boto3>=1.34
pyarrow>=15.0
